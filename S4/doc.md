# Вариант №4
# Сервис управления разрешениями (Permission Service)

## Сущность: Permission (Разрешение)

Тонкая настройка прав: кто может редактировать расписание, кто только смотреть, кто может назначать замены.

---

### 1. Информация для создания сущности

`POST /permissions`

| Параметр | Пояснение | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|-----------|----------------|-----|-------------|-----------------------|
| `name` | Название разрешения | Да | str | длина ≤ 100, уникально | — |
| `description` | Описание разрешения | Нет | str | длина ≤ 255 | `''` |

### 2. Информация, возвращаемая при успешном создании

| Параметр | Тип | Пояснение |
|----------|-----|-----------|
| `id` | int | Идентификатор |
| `name` | str | Название |
| `description` | str | Описание |
| `is_active` | bool | Флаг активности (по умолчанию true) |

---

## Изменить сущность по ID

### 3. Информация для изменения

`PUT /permissions/{id}`

| Параметр | Пояснение | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|-----------|----------------|-----|-------------|-----------------------|
| `name` | Новое название | Нет | str | длина ≤ 100, уникально | — |
| `description` | Новое описание | Нет | str | длина ≤ 255 | — |

> Поле `is_active` не может быть изменено через данный эндпоинт.

### 4. Информация, возвращаемая при успешном изменении

| Параметр | Тип |
|----------|-----|
| `id` | int |
| `name` | str |
| `description` | str |
| `is_active` | bool |

---

## Удалить сущность по ID (soft delete)

`DELETE /permissions/{id}`

**Возвращаемое значение:** `True`, если статус `is_active` изменен на `false`, иначе `False`.

---

## Получить сущность по ID

`GET /permissions/{id}`

---

## Получить список сущностей по заданным параметрам

`GET /permissions`

---

## Сущность: RolePermission (Связь роли и разрешения)

### 1. Информация для создания связи

`POST /role-permissions`

> **Примечание:** Связь можно создать только с активным разрешением (`is_active: true`).

| Параметр | Пояснение | Обязательность | Тип |
|----------|-----------|----------------|-----|
| `role_id` | ID роли | Да | int |
| `permission_id` | ID разрешения | Да | int |

### 2. Информация, возвращаемая при успешном создании

| Параметр | Тип |
|----------|-----|
| `id` | int |
| `role_id` | int |
| `permission_id` | int |

---

## Получить разрешения по ID роли

`GET /role-permissions/by-role/{role_id}`

**Возвращаемый список:** список объектов `Permission` (id, name, description, is_active), связанных с данной ролью.

---

## Точки входа REST API

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/permissions` | Создать разрешение |
| GET | `/permissions/{id}` | Получить разрешение по ID |
| GET | `/permissions` | Получить список разрешений |
| PUT | `/permissions/{id}` | Обновить разрешение |
| DELETE | `/permissions/{id}` | Мягкое удаление разрешения |
| POST | `/role-permissions` | Назначить разрешение роли |
| GET | `/role-permissions/{id}` | Получить связь по ID |
| DELETE | `/role-permissions/{id}` | Удалить связь по ID |
| DELETE | `/role-permissions` | Удалить связь по role_id и permission_id |
| GET | `/role-permissions/by-role/{role_id}` | Получить разрешения для роли |

---

## ER-диаграмма

ER-диаграмма представлена в файле `erd.png`:

![ER-диаграмма](erd.png)